import { ExtensionContext } from "vscode";

/**
 * The initialization function called when the extension starts. This is currently not implemented
 * since coq-lsp cannot be run as a webworker.
 */
export function activate(_context: ExtensionContext): void {
}

export function deactivate() {
}
