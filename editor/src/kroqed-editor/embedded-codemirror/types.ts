
// Enum for the movement unit.
export const enum MovementUnit {
	line = "line",
	character = "character"
}

// Enum for the movement direction.
export const enum MovementDirection {
	forward = +1,
	backward = -1
}